version: '3'

services:
  serverlso:
    build:
      context: ./Server
      dockerfile: dockerfile  # Assicurati che il nome del tuo file Dockerfile sia corretto
    container_name: serverlso
    ports:
      - "5050:5050"

  clientlso:
    build:
      context: ./Cliente
      dockerfile: dockerfile  # Assicurati che il nome del tuo file Dockerfile sia corretto
    container_name: clientlso
